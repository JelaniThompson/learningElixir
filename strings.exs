name = "Jelani"

# Interpolation/literals
IO.puts "Hello, #{name}"

# Concatenation
IO.puts "Hello, " <> name

# The string module also contains a plethora of helpful functions
IO.puts String.length("Rick_and_Morty")
IO.puts String.upcase("meow!")

# The IO module is used to write to standard input/output